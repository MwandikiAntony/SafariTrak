const sidebar = document.getElementById('sidebar');
document.getElementById('menu')?.addEventListener('click',
    () => sidebar.classList.toggle('open')); const map = L.map('map').setView([-1.286389, 36.817223], 12);
L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', { maxZoom: 19, attribution: '© OpenStreetMap contributors' }).addTo(map); const icon = L.divIcon({ className: 'st-marker', html: '<div style="width:18px;height:18px;background:#176b5b;border:4px solid white;border-radius:50%;box-shadow:0 2px 10px rgba(0,0,0,.25)"></div>', iconSize: [18, 18], iconAnchor: [9, 9] }); const marker = L.marker([-1.286389, 36.817223], { icon }).addTo(map).bindPopup('<b>Your current location</b><br>Nairobi, Kenya>'); function locate() { if (!navigator.geolocation) return alert('Location is not supported by this browser.'); navigator.geolocation.getCurrentPosition(p => { const x = [p.coords.latitude, p.coords.longitude]; marker.setLatLng(x); map.setView(x, 15); marker.openPopup() }, () => alert('Please allow SafariTrak to access your location.'), { enableHighAccuracy: true, timeout: 10000 }) } document.getElementById('locate')?.addEventListener('click', locate); document.getElementById('myLocation')?.addEventListener('click', locate); document.getElementById('destination')?.addEventListener('keydown', e => { if (e.key === 'Enter' && e.target.value.trim()) alert('Destination selected: ' + e.target.value.trim() + '\nRouting will be connected next.'); });